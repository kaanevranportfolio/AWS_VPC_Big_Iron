

    server {
        listen       {{ nginx_port }};
        listen [::]:{{ nginx_port }};


        location / {
            proxy_pass http://{{ webserver_private_ip }}:{{ webserver_port }};
            include /etc/nginx/proxy_params; # use include for reusable proxy parameters
        }
    }

